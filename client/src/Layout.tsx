import { Outlet } from "react-router-dom";
import { Container } from "semantic-ui-react";

const Layout = () => {
  return (
    // <Container>
    <Outlet />
    // </Container>
  );
};

export default Layout;
